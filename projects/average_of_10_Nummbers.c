#include <stdio.h>
#include <stdlib.h>
#include <Windows.h>

int main(){
    system("cls");
    int a[10],sum;
    float avg;
    printf("Enter 10 numbers");
    for(int i=0;i<=9;i++)
       scanf("%d",&A[i]);
    
    for ( i = 0; i <=9; i++)
    {
        /* code */
        sum = sum +a[i];
    }
    avg = sum/10.0
    printf("Average is %f",avg);
}